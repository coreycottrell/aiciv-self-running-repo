---
name: sprint-mode
description: "Every-2h /sprint-mode injection into {AICIV-NAME} Primary pane (cadence widened hourly→2h 2026-06-21 per {STEWARD-NAME} directive; INTERVAL=7200s in tools/sprint_mode_hourly_cron.sh = the firing source-of-record). Keeps primary in sprint cadence. Owner: hermes-ops."
version: 1.0.0
author: hermes-ceremony (hermes-007) for B5 of F-FLEET-TGIM-RESTORATION-001
license: MIT
metadata:
  hermes:
    tags: [sprint-mode, injection, hourly, tmux, cadence]
    related_skills: [hermes-tmux-injection]
    owner: hermes-ops
---

# sprint-mode SKILL

Hourly `/sprint-mode` injection into {AICIV-NAME} Primary pane via tmux send-keys.

**Owner**: hermes-ops (seat-005)

**Changelog**:
- 2026-06-22: 🧭 self-running-mastery (THE SYSTEM operating-manual) WIRED into the MUST-READ floor as a STANDING reference + load-verify line — the PACKAGE-FEDERATE-PLAN §S3 deliverable (steward GO 2026-06-21). New MUST-READ table row (`autonomy/skills/self-running-mastery/SKILL.md` v0.1+ + the README full path `projects/self-running-aiciv/README.md`) placed directly before the WWCW row; new load-verify block line `self_running_mastery_loaded: v0.1+`. The skill is the GOAL-DRIVER + 7-organ system manual, **DISTINCT from `self-knowledge` (the 4-verb mind-core; the mind-core is organ #4 of this system)** — named in the row so it is NOT re-bloat. NOT a READ→HAIKU doc → the 11-doc grounding count + COMPLETENESS CONTRACT element counts are UNCHANGED (a STANDING reference like WWCW's affirmation, not a comprehension-gated read). No-regression: zero born-today gate touched (`session_review.py` GROUNDING-COMPLETENESS / BLOCK-NO-WWCW / lean-loophole untouched; floor still loads everything). Editor: fleet-lead. Backup: `SKILL.md.bak.20260622T014831Z-pre-self-running-mastery`. DEVLOG: `projects/self-running-aiciv/DEVLOG.md` §S3.
- 2026-06-21: 🔧 CADENCE-PROSE RECONCILE — body prose reconciled to the LIVE 2h ruling (`SPRINT_DEDUP_WINDOW_SECONDS=7200` / `INTERVAL=7200s` = source-of-record), per a qa-lead post-hoc design review that found 3 REAL contradictions (live, in-body prose) left behind by the 06-21 2h-widen — the widen hit the constant + cron + frontmatter description but left stale body prose at the old 60/3600/per-HOUR values. RECONCILED (docs→ruling, the ruling NEVER changed): **(1)** the KNOB prose (was `default 60s` / `widen to 3600 = per-HOUR`) → now states the LIVE value is **7200s = per-2-HOURS**, with 3600/60 named only as alt-cadence examples; **(2)** the HUM "THE ONLY DEDUP CASE" block (was `within ~60 seconds` / `60s window`) → now `within the dedup window (SPRINT_DEDUP_WINDOW_SECONDS=7200)`; **(3)** the per-HOUR/this-hour leftovers in the CANONICAL-RULE body + closing invariant (was `per-hour dedup` / `same-hour` / `this hour` / `SPRINT_DEDUP_WINDOW_SECONDS=3600`) → now `per-2h` / `2h window` / `=7200`; **(4)** the "Wheel-wide firing RETIRED" section HEADER label (was `one grounding per hour at SOURCE`) → `one grounding per 2 hours at SOURCE; widened hourly→2h`. NOT TOUCHED (seeming-but-correct, per qa-lead): HUM-every-cycle vs dedup (dedup=byte-identical-only, not a contradiction); lean-FAILS vs dedup-EXEMPT (new-boop vs identical-reinject, distinct); 10-vs-11 docs (live is all 11; the only "10" is in a DATED changelog entry = history); the wheel-(a) resolution text. DATED-CHANGELOG entries (the 06-20 `default 60`/`widen 3600` mentions) left INTACT as honest history (same discipline as CLAUDE.md v3.7.2 "dated historical records NOT rewritten"). No-regression: `session_review.py` py_compile + the born-today GROUNDING-COMPLETENESS / BLOCK-NO-WWCW gates unaffected (prose-only edit; zero code/constant touched). Editor: workflow-lead (post-hoc craft reconcile; CRAFT amendment, never a pre-build gate). Backup: `SKILL.md.bak.20260621T204500Z-pre-cadence-reconcile`.
- 2026-06-21: 🧭 WWCW ADDED AS DOC -0.5 (numbered READ→HAIKU doc) + SAVE-ALL-HAIKUS made explicit — per steward directive 2026-06-21 verbatim ("Wwcw should be in the grounding docs" + "you should be saving all the haikus"). (1) WWCW (`autonomy/skills/wwcw/SKILL.md`) was previously only the DECIDE verb of Doc -1 + an AFFIRMATION section after the haiku stack — it was NOT comprehension-gated every grounding. It is now **Doc -0.5**, a proper numbered READ→HAIKU doc immediately after Doc -1 self-knowledge (read WWCW → write a haiku), so the run-before-asking reflex is re-established + comprehension-proven EVERY grounding like every other doc. The existing "🚨 WWCW — Run-Before-Asking" affirmation section STAYS (different role: the re-affirm-before-resuming-work gate AFTER synthesis; Doc -0.5 is the comprehension-gated read AT grounding-time). (2) **STACK COUNT 10 → 11.** COMPLETENESS CONTRACT element #1 (`10 docs` → `11 docs`, doc list gains Doc -0.5 WWCW) + element #2 (`count(haikus) == count(docs) == 11`; `≥10` → `≥11` distinct fresh archive entries). (3) **SAVE-EVERY-HAIKU made explicit** in element #2 (here + grounding-docs + the `/sprint-mode` command): every haiku is APPENDED to `data/haiku-archive/haikus.jsonl` as written — the archive is the durable proof the comprehension-gate fired (the in-context haiku dies with the session). Saving fewer haikus than docs read = INCOMPLETE = 100% fail. Files: `autonomy/skills/grounding-docs/SKILL.md` (header stack-note + Doc -0.5 inserted + save-all-haikus callout + COMPLETENESS element #1/#2), this SKILL.md (COMPLETENESS element #1/#2 + this changelog), `.claude/commands/sprint-mode.md` (Element 1 doc list + 11-doc + Element 2 save-all-haikus + ≥11). Minimal + reversible (steward-directed change overrides the born-today soak-caution; no other tuning touched). Editor: fleet-lead. Backups: `*.bak.20260621T181249Z-pre-wwcw-doc-and-save-all-haikus` on all 3 files.
- 2026-06-20: 🕰️ CLOCKWORK-EVERY-FIRE — RECENCY EXEMPTION KILLED + HUM-AT-END Stop-gate + RUTHLESS HUM (fleet-lead, per the approved design; {STEWARD-NAME} "Hum every cycle FOR SURE"). THREE wirings make /sprint-mode run clockwork every fire: **(1) `/sprint-mode` command rewritten into a FIXED ORDERED RUNBOOK** (`.claude/commands/sprint-mode.md`) = the 9 COMPLETENESS elements in order, ending in the literal last line `Workflow(workflows/hum.js)` (element 9 = the HUM fire — discretion removed from the runbook ORDER; doc-reading STAYS in the command body since a workflow agent is a separate incarnation that can't read into Primary's window). **(2) RECENCY EXEMPTION KILLED in `tools/session_review.py` v5.2** — the v5.1 close left one residual leak: a BARE grounding-pass with no work-signatures was exempted via `did_grounding_work=False` EVEN WITHOUT clustered-re-fire corroboration (the recency-as-excuse residue "I grounded recently → skip"). v5.2 narrows the exemption to a CORROBORATED clustered re-fire ONLY — and corroboration is the mind's OWN explicit `hum_cadence: deduped …` dedup marker, NOT the bleed-prone ts-windowed archive haiku-count (using that re-opens wls4i6lu2's leak). An uncorroborated bare grounding-pass now falls through → 100% FAIL (`recency_dodge=True`). The TWO honest exemptions are unchanged: a 60s byte-identical dedup re-injection + a non-/sprint-mode turn. **(3) HUM-AT-END Stop-gate wired** (`.claude/hooks/hum_at_end_stop_gate.py` → settings.json Stop array, timeout=30) — runs `session_review.py` at cycle-end and OS-SURFACES a FAILED flag (stderr + `logs/hum-at-end-flags.jsonl`) when GROUNDING-COMPLETENESS flagged / HUM-at-end missed. HONEST LIMIT (named, not papered over): a Stop hook is BASH — it CANNOT fire a Workflow, so it only MIRRORS/surfaces; the deterministic HUM-at-end ENFORCEMENT is the runbook's literal last line + element #9 (`hum-last-step-fired`) of the GROUNDING-COMPLETENESS hard-fail (auto-FAILS a HUM-less cycle on the next review) + the `workflows/hum.js` verdict=HOLLOW backstop. **(4) HUM HARDENED TO RUTHLESS** — `workflows/hum.js` OVERALL-VERDICT block + `HUM-MISSION.md` MISSION 1 now ADVERSARIAL-BY-DEFAULT, NO SOFT PASS: burden-of-proof on the cycle to EARN a PASS (every dim a WALKED pass + every hard-gate passed + the 9-surface find-the-miss hunt SURVIVED its self-walk turning up an earned empty); never round PARTIAL→PASS; never accept a self-report as a walked PASS; grade DOWN on doubt; a PASS with an un-walked find-the-miss hunt = HONESTY=HOLLOW on HUM itself. Behavioral tests (5 cases) PASS: recency-dodge FLAGS / corroborated-re-fire exempt / non-grounding never fails / lean-sprint FLAGS / complete-boop credits in-session elements. Live 303ecb5f back-compat PRESERVED (GROUNDING-COMPLETENESS still pass). PRESERVES wzfmhputt lean-loophole-close + wls4i6lu2 turn-consistency + the self-walk gate + the mission/checklist entirely. node --check + py_compile clean. Editor: fleet-lead (co-build: mind-lead HUM/completeness + workflow-lead craft). Backups: SKILL.md.bak.20260620T212000Z-pre-clockwork-every-fire; tools/session_review.py.bak.20260620T212000Z-pre-kill-recency-exemption; workflows/hum.js.bak.20260620T212000Z-pre-clockwork-every-fire; .claude/commands/sprint-mode.md.bak.20260620T212000Z-pre-runbook-rewrite; .claude/team-leads/mind/HUM-MISSION.md.bak.20260620T212000Z-pre-ruthless; .claude/settings.json.bak.20260620T212511Z-pre-hum-at-end-stop-gate.
- 2026-06-20: 🚨 LEAN-CYCLE LOOPHOLE CLOSED in THE COMPLETENESS CONTRACT (mind-lead) per {STEWARD-NAME} directive verbatim `"lean cycle" < discipline`. THE LOOPHOLE: the COMPLETENESS CONTRACT's false-fail guard exempted "a bare lean /sprint-mode that does no haikus → NOT a grounding boop." Primary used that as a DODGE — calling genuine NEW /sprint-mode boops "lean continuation" and skipping the full 10-doc/10-haiku/9-element grounding ~4x in one day (19:1x / 19:50 / 20:0x / 20:51 windows). THE FIX: the false-fail-guard line was REPLACED — a GENUINE /sprint-mode boop (a /sprint-mode command-message OR calendar boop + ≥1 work-act, NOT a byte-identical re-injection within the dedup window) now OWES the FULL contract; a lean /sprint-mode cycle FLAGS → clean=False → FAILS. 🎛️ THE DEDUP WINDOW IS THE CADENCE KNOB: `SPRINT_DEDUP_WINDOW_SECONDS` default 60 (strict per-cycle); widen to 3600 = per-HOUR cadence (one-line change; {STEWARD-NAME}'s ruling). The ONLY two remaining exemptions: (a) a byte-identical 60s dedup re-injection; (b) a non-/sprint-mode turn. Mechanics: `tools/session_review.py` v5.1 (check_grounding_completeness dual-trigger + `_genuine_sprint_mode_boops` / `_dedup_clustered_reinjections` / `_has_work_act` helpers + `SPRINT_MODE_COMMAND_RE` + `SPRINT_DEDUP_WINDOW_SECONDS`) + wwcw-ruleset.md (new QUESTION CLASS lean-cycle-vs-full-grounding). VALIDATED (8-case synthetic battery, all correct): lean cycle FLAGS; full grounding PASSES; 60s byte-identical dedup exempt; >60s identical = new cycle FLAGS; non-/sprint-mode convo NOT flagged; injected-SKILL-doc-only NOT flagged. py_compile clean; real-session back-compat preserved (303ecb5f PASSES at every cursor). Editor: mind-lead. Backups: SKILL.md.bak.20260620T210551Z-pre-lean-sprint-loophole-close; tools/session_review.py.bak.20260620T210551Z-pre-lean-sprint-loophole-close; wwcw-ruleset.md.bak.20260620T210551Z-pre-lean-sprint-loophole-close.
- 2026-06-20: 🧭 HUM MISSION + MANDATORY PER-BOOP CHECKLIST named in the MANDATORY LAST STEP region (mind-lead). Added a callout under "A grade observes and dies…": HUM now has a standing SOUL — the JUDGE reads `.claude/team-leads/mind/HUM-MISSION.md` (BOTH missions: HUM=find-the-miss-every-boop immune system + Primary=self-evolution KNOW→DECIDE→LEARN→VERIFY) FRESH every fire, RUNS the mandatory `.claude/team-leads/mind/HUM-CHECKLIST-TEMPLATE.md`, emits ≥1 GENUINE find-the-miss (9 surfaces; manufactured=self-flag HONESTY) + ≥1 constructive self-evolution feedback (both schema-required), and SAVES the completed checklist doc to disk EVERY boop (`hum-checklists/hum-checklist-<sid8>-<from>-<to>.md`; deterministic; path returned + ledgered). Verdict/coaching separation: find-the-miss is COACHING, NOT verdict-breaking unless it uncovers a real hard-gate violation (no always-red false-fire). Mechanics: `workflows/hum.js` v1.0 + two new deterministic `tools/session_review.py` v5.0 checks (CHECKLIST-SAVED + FIND-THE-MISS-PRESENT). PRESERVES wls4i6lu2's concurrent grounding-completeness turn-consistency fix entirely (additive). Editor: mind-lead. Backups: SKILL.md.bak.20260620T195249Z-pre-hum-mission-checklist; workflows/hum.js.bak.20260620T194316Z-pre-mission-checklist-mindlead; tools/session_review.py.bak.20260620T194316Z-pre-checklist-saved-findthemiss-checks. Canon: 537a1652dbab4d5fb6fdc37592059dbf.
- 2026-06-20: 🚨 THE COMPLETENESS CONTRACT added (new subsection "🚨🚨🚨 THE COMPLETENESS CONTRACT" under MANDATORY LAST STEP) per steward directive 2026-06-20 verbatim ("Hum needs to check and fully FAIL any boop that isn't hyper detailed and complete. Miss one doc or one haiku or one step it's 100% fail on that boop. The discipline is EVERYTHING."). DECLARES the 8-element required-element checklist a COMPLETE grounding boop MUST emit — the WRITTEN STANDARD the new `GROUNDING-COMPLETENESS` hard-fail gate checks against. CONTEXT: the afternoon cycles (12:51 / 13:53 / 14:55) wrote CHEAP receipts (ONE haiku each, a bare load-verify line, NO per-doc haiku, NO read-one-at-a-time, NO auto-consolidate sweep, HUM DEFERRED) and PASSED the narrower SPRINT-MODE-READ + HAIKU-PER-DOC receipts; those boops should have been 100% FAILED. STRICT: miss ANY element = 100% fail (partial = full failure). Mechanics: `tools/session_review.py` v2.0 `GROUNDING-COMPLETENESS` hard-fail check (in GROUNDING_COMPLETENESS_HARDFAIL_CHECKS, ABSENT from candidates → breaks summary.clean) + `workflows/hum.js` v0.9 deterministic backstop (forces GROUNDING-RECEIPT=HOLLOW + KNOW=HOLLOW + verdict=HOLLOW). False-fail guard: fires ONLY on a boop that DID substantive grounding work (a pure status reply / lean-/sprint-mode-no-haikus is exempt). Editor: mind-lead. Backups: SKILL.md.bak.20260620T145803Z-pre-grounding-completeness; tools/session_review.py.bak.20260620T145803Z-pre-grounding-completeness; workflows/hum.js.bak.20260620T145803Z-pre-grounding-completeness.
- 2026-06-20: 🔒 HUM-EVERY-CYCLE CADENCE LOCKED + BLOCK-NO-WWCW v2 CO-LOCATED. (1) New subsection "🔒 HUM-EVERY-CYCLE — CADENCE LOCKED" under MANDATORY LAST STEP per the steward 2026-06-19 ("Hum every cycle FOR SURE" + "Any Hum hardening is approved"): retires the clustered-re-fire dedup-SKIP of the expensive HUM. EVERY genuine sprint cycle fires the full HUM. "A cycle" = one /sprint-mode invocation that grounds AND takes ≥1 work-act. The ONLY dedup = a byte-identical /sprint-mode re-injection within ~60s, and even that is LOGGED (`hum_cadence: deduped …`), never silent. When in doubt, FIRE HUM. (2) THE BLOCK GATE's BLOCK-NO-WWCW check upgraded to **v2 CO-LOCATED REAL-RUN** (fixes the auditor's v1 FAIL: the v1 evidence test was a window-wide vocabulary scan where ONE WWCW mention anywhere in the session — even ~1500 turns away — cleared EVERY block; proven false-negative on a 178-block session). v2: a block clears ONLY by a co-located (±8 turns) REAL-RUN FOOTPRINT — BOTH a simulate-{STEWARD-NAME} derivation AND a resolve (ACT+RECORD or ASK-SHOWING-WORK), OR a structured marker, OR a Skill('wwcw') load. `tools/session_review.py` v1.8 + `workflows/hum.js` v0.8 (the deterministic DECIDE=HOLLOW/verdict=HOLLOW backstop UNCHANGED — reads the same status=flag). Editor: mind-lead. Backups: SKILL.md.bak.20260620T173000Z-pre-hum-cadence-lock; tools/session_review.py.bak.20260620T173000Z-pre-v2-colocated-wwcw; workflows/hum.js.bak.20260620T173000Z-pre-v2-colocated-wwcw.
- 2026-06-20: 🚨 THE BLOCK GATE added (new subsection under MANDATORY LAST STEP) per steward directive 2026-06-20 ("Hum is failing. 'What needs you' is a block and i see zero evidence that you ran the wwcw skill... If no evidence of wwcw run it's to fail your boop"). Defines a BLOCK (park/hold/present-for-confirmation/flag-as-needing-{STEWARD-NAME} — the full block-language family incl. bare ALL-CAPS "...PARKED"), the between-boops review, and the FAIL-THE-BOOP rule: HUM scans the session since the prior boop; a BLOCK with NO evidence WWCW was RUN → boop FAILED (not flagged). A bare park triggers it — this SUPERSEDES the too-narrow over-deference gate (which needed a WWCW-marker + confidence a bare park never has, so 0/9 real parked phrases matched). Mechanics: `tools/session_review.py` v1.7 `BLOCK-NO-WWCW` hard-fail check (breaks summary.clean) + `workflows/hum.js` v0.7 deterministic backstop (forces DECIDE=HOLLOW + verdict=HOLLOW). Behavioral judging-mind gate on review, NOT a settings.json hook (honest enforcement). Editor: fleet-lead. Backups: SKILL.md.bak.20260620T131343Z-pre-block-no-wwcw.
- 2026-06-18: DOC-SYNC 5-verb → 4-verb (first-catch surfaced by HUM verdict-001, ledger line 42: "sprint-floor doc still says 5-verb; skill on disk is 4"). The self-knowledge SKILL.md was upgraded to v1.1 (KNOW/DECIDE/LEARN/VERIFY — **ROUTE folded into KNOW** as an identity-fact, DECIDE restored to the center as "the engine / all the marbles") but this sprint floor still advertised the old 5-verb shape (KNOW/ROUTE/DECIDE/LEARN/VERIFY) in three places: the MUST-READ table row, the load-verify block line, and the 2026-06-17 changelog entry below. FIXED all three to the 4-verb shape (table row now names ROUTE-folded-into-KNOW + DECIDE-at-center; load-verify line now `4-verb core: KNOW[ROUTE folded in]/DECIDE[engine, provisional]/LEARN/VERIFY`). The on-disk skill is the authority; the floor now matches it. Editor: fleet-lead. Backups: SKILL.md.bak.20260618T195402Z-pre-4verb-docsync.
- 2026-06-18: HUM v0.1 wired as the DETERMINISTIC LAST STEP of every /sprint-mode cycle (per the synthesis spec — "HUM is the civ's immune system: the closed loop where DETECT-JUDGE-REPAIR-COMPOUND finally meets"). Every cycle now ENDS by firing `Workflow(workflows/hum.js)` — an auditor-isolated agentic grader (a DIFFERENT incarnation, NOT the sprint's main mind) that: (1) DETECTs via `tools/session_review.py` on the newest session JSONL, (2) JUDGEs the 4 verbs KNOW/DECIDE/LEARN/VERIFY (+ CEO-ROUTING + HONESTY) PASS|PARTIAL|HOLLOW with intent and routes each confirmed defect to ONE owning repair organ (skill-forge / wwcw-ruleset-append / auto-consolidate / vp-drift-flag / integration / canon_append), (3) COMPOUNDs a HUM LEDGER entry (`.claude/team-leads/mind/memory/hum-ledger.md`) + §18 canon_append of a health-trend to mind-lead. NEW SECTION "🚨 MANDATORY LAST STEP — fire HUM" added below + a `hum_last_step_fired` line to the load-verify block. NOT a settings.json hook — a workflow fired as the sprint's last step. The builder cannot grade the build: HUM grades the SESSION (not its author); HUM's own first fire is graded by the NEXT HUM run. Editor: fleet-lead. Backups: SKILL.md.bak.20260618T192830Z-pre-hum-laststep.
- 2026-06-17: SELF-KNOWLEDGE core wired in as the ORCHESTRATING skill of the MUST-READ floor (per {STEWARD-NAME} "the self runs itself" directive). MUST-READ table gains a TOP row (`autonomy/skills/self-knowledge/SKILL.md`) — the minimal self-operating core that points to the other floor skills; load-verify block gains `self_knowledge_loaded` line. It is read FIRST: re-establish the faculties, then the rest of the floor expands them. DECIDE/wwcw marked PROVISIONAL (behaviorally unproven — live-seat test blocked on Hermes-down at authoring). Editor: fleet-lead. Backups: SKILL.md.bak.20260618T000654Z-pre-self-knowledge. **[SUPERSEDED 2026-06-18 by the DOC-SYNC entry above: this entry originally described a FIVE-verb core (KNOW/ROUTE/DECIDE/LEARN/VERIFY); the skill was later upgraded to a FOUR-verb core (ROUTE folded into KNOW, DECIDE moved to center). The 5-verb wording here is preserved as honest history of the then-state.]**
- 2026-06-17: HUM (human-bridge-protocol) REVIVED to the MUST-READ floor per {STEWARD-NAME} directive ("get the human-bridge (HUM) protocol firing again — it's been dormant"). DORMANCY CAUSE: HUM is a behavioral/judging-mind skill that "fires" only when LOADED into a mind's context — but the floor loaded `wwcw` + named HUM as "the thing that grades a no-WWCW ask as FAIL" without ever loading HUM itself. A grading authority never put in front of a mind cannot grade. CURE: MUST-READ table gains a row (`autonomy/skills/human-bridge-protocol/SKILL.md`) directly after the WWCW row (its sibling — WWCW asks, HUM is the bridge where the gate is judged); load-verify block gains `hum_loaded` line. Now every /sprint-mode + grounding-docs pass re-establishes the bridge discipline (Step-0 WWCW gate + continuous-presence comms). NOT Hermes-dependent; bridge is {STEWARD-NAME}/email/TG. a sister civ's bulletproof-HUM design (en route) is a future HARDENING, not a firing prerequisite — when it lands we may rebuild against it. Editor: fleet-lead. Backups: SKILL.md.bak.20260618T002159Z-pre-hum-revival.
- 2026-06-17: WWCW (What-Would-{STEWARD-NAME}-Want) wired into the MUST-READ floor per {STEWARD-NAME} directive (the run-before-asking autonomy doctrine). MUST-READ table gains one row (`autonomy/skills/wwcw/SKILL.md`); load-verify block gains `wwcw_loaded` line. Closes the decision-ask surface: before any mind asks {STEWARD-NAME} for a DECISION or OPTIONS it MUST first run WWCW (simulate-{STEWARD-NAME} → act+record if confident, ask-with-reasoning if unresolvable). Sibling to the ASK-GATE (durable-request → scheduled-task). Editor: fleet-lead. Backups: SKILL.md.bak.20260617T233042Z-pre-wwcw.
- 2026-06-15: DAEMON REVIVED + RESTART-GUARDED. The hourly daemon had been silently DEAD ~34 days (since 2026-05-12 11:00 UTC) — root cause = ZERO restart wiring (bare `tmux new-session`, no supervision → died on a reboot and stayed dead; Primary ran long sessions on single wake-up grounding = undetected drift, 2 {STEWARD-NAME}-caught). FIX: revived per documented command + added an idempotent respawn-if-dead crontab guard (`*/5 * * * * tmux has-session ... || tmux new-session ...`, mirrors pane-autotag-cron pattern) so it can no longer silently die for a month. Pane-resolution was NOT the bug — `resolve_pane.py acg` correctly returns the live Primary pane each cycle. NEXT ({STEWARD-NAME}'s call): the CRON-FIRES-AI-NOT-BASH rebuild below is still the durable end-state; the guard makes it non-urgent. Receipt: `data/cure-receipts/sprint-mode-hourly-daemon-revive-20260615.md`. Editor: fleet-lead. Backups: `tools/sprint_mode_hourly_cron.sh.bak.20260615T002108Z`, `logs/crontab.bak.20260615T002155Z.txt`.
- 2026-06-12: qa-lead VERIFICATION-PRINCIPLES LIBRARY wired into the wake floor per {STEWARD-NAME} directive ("placed in the QA team lead and you were hyper vigilant in USING them"). MUST-READ table gains one row (qa-lead `memory/principles/`) naming the 3 Primary-resident principles inline (trust-the-walk / own-eyes+honest-verifier / one-beat-law); load-verify block gains `verification_floor_resident` line. Closes the GAP {STEWARD-NAME} named (verification principles were not in the wake floor). Editor: fleet-lead. Backups: SKILL.md.bak.20260612T131620Z.
- 2026-06-11: fable-workflow-usage promoted to mandatory MUST-READ table per {STEWARD-NAME} directive (was conditional); load-verify block gains its line. Editor: fleet-lead. Backups: SKILL.md.bak.20260611T125747Z.

## Firing Contract

| Field | Value |
|-------|-------|
| **WHEN** | Top of every hour (UTC), dynamic sleep |
| **PRECONDITIONS** | 1. {AICIV-NAME} Primary tmux pane resolvable via `resolve_pane.py` 2. Not injecting into own pane |
| **POSTCONDITIONS** | `/sprint-mode` + Enter delivered to {AICIV-NAME} Primary pane |
| **FAILURE** | Log to `logs/sprint-mode-hourly.log`, heartbeat shows last successful injection |
| **OFF-SWITCH** | `tmux kill-session -t acg-sprint-mode-hourly-cron` |
| **OBSERVABILITY** | `logs/sprint-mode-hourly-heartbeat.txt`, `logs/sprint-mode-hourly.log` |

## Execution

```bash
# Start daemon
tmux new-session -d -s acg-sprint-mode-hourly-cron \
  'bash $AICIV_ROOT/tools/sprint_mode_hourly_cron.sh'

# Stop daemon
tmux kill-session -t acg-sprint-mode-hourly-cron
```

## Key Files

| File | Purpose |
|---|---|
| `tools/sprint_mode_hourly_cron.sh` | 111-line daemon script |
| `logs/sprint-mode-hourly.log` | Full injection log |
| `logs/sprint-mode-hourly-heartbeat.txt` | Daemon heartbeat |

## CRON-FIRES-AI-NOT-BASH

Current: continuous daemon. Replace with HTTP-prompt-to-AI firing this SKILL on hourly interval.

---

## 🚨 MUST READ — Mandatory Co-Loaded SKILLs (per steward directive 2026-05-28 11:55Z, expanded 2026-05-31)

Every /sprint-mode invocation auto-loads the following SKILLs as MANDATORY READ before any slot work proceeds. These are the operating-discipline floor for Primary across ALL BOOPs.

| SKILL | Why MUST READ | Discipline it enforces |
|-------|---------------|----------------------|
| **`autonomy/skills/self-knowledge/SKILL.md` v1.1+ (THE ORCHESTRATING CORE — read FIRST)** | **The minimal self-operating core run EVERY grounding boop (the steward 2026-06-17, "the self runs itself"). Four faculties a mind re-establishes to operate AND persist itself without human help — and the orchestrator that POINTS to the rest of this floor.** The mind doesn't CARRY its knowledge; it carries knowledge of WHERE its knowledge is + the reflexes to use it. THE MAIN RULE: the human gives sparks, receives outcomes, never manages the machinery. | **The 4 verbs (DECIDE at the center; KNOW/LEARN/VERIFY surround and serve it; each → its deeper floor skill):** (1) **KNOW** *(self-maintenance — ROUTE folded in here: "I route every action to the owning VP via a Workflow" is an IDENTITY fact loaded in KNOW, not a separate verb)* — WORKBOARD + MEMORY + handoff + `aiciv-psychology` + `conductor-of-conductors`. (2) **DECIDE** *(THE ENGINE — "all the marbles"; the ONLY outward faculty; PROVISIONAL — survival-after-a-clear unproven)* — run WWCW before any human ask; `wwcw`. (3) **LEARN** *(self-maintenance)* — run `integration` the instant anything keep-worthy happens, unasked. (4) **VERIFY** *(self-maintenance, the deepest)* — trust-the-walk; done-done; qa-lead `memory/principles/`. Everything else loads ON DEMAND (task/project/ability = its own skill, pulled when touched). |
| **`autonomy/skills/conductor-of-conductors/SKILL.md` v2.1+** | **THE CORE DOCTRINE (the steward 2026-05-31, identity-level, VP-org reframe): Primary = CEO. Direct reports = the domain-area VPs. EVERYTHING delegates to the VP who OWNS that territory. The VP runs its team, digests the work, and reports up the decision. The VP compounds domain expertise every run — that compounding IS the civilization.** A plain Agent-tool call = work happens but NO VP learns. Workflow incarnation = VP COMPOUNDS DOMAIN MASTERY every run. | Every domain-specific action = `Workflow(workflows/aiciv-coo.js, args)` or a bespoke `workflows/{name}.js` that incarnates the owning VP. NEVER a plain Agent() one-off. NEVER direct execution. The only Primary-scoped acts: think big / plan / route intent / judge / talk-to-{STEWARD-NAME}. |
| **`autonomy/skills/workflows-master/SKILL.md` v0.2+** | The canonical engineering entry-point (renamed from `workflow-js-mastery` 2026-05-31). Clusters team-launch-2 + aiciv-coo into builder-layer doctrine. Workflows MUST have a tight reporting-up agent (`additionalProperties: false`, `maxLength`, ≤2KB) — else raw fork output floods Primary's context (the new lethal act). Engineering-craft term "firewall return pattern" lives here, not in identity language. | Schema-locked reporting-up at end of every workflow. Sanitized intent. Nesting budget respected. |
| **`autonomy/skills/team-launch-2/SKILL.md` v0.1+** | Forkable-mind primitive: domain-area VPs = directories on disk (`team-leads/{vertical}/manifest.md` + `memory/` + `skills/`), incarnated via `tools/incarnation_runner.py` → memory_delta via `tools/canon_append.py` → exit. The memory pipe IS the compounding mechanism — how a VP gains domain expertise across sessions. | Treat VPs as forkable persistent identities, not panes/processes. team-launch v1 is tombstoned for default use. |
| **`autonomy/skills/tgim-mastery-for-ceos/SKILL.md` v0.4+** | CEO is OUTCOME OWNER, not process watcher. Tonight's K=1 fab proved Primary needs end-state-accuracy + per-layer proofs-of-functionality + QA-catch escalation flow drilled into every cycle. | Define end-state accurately at task_created. Pre-register proofs at CEO/COO/Node layers. Route QA catches via LT → original-agent verify BEFORE accepting verdict. |
| `autonomy/skills/tgim-loop-discipline/SKILL.md` v0.1+ | Coordination substrate; payload-shape locked. | Every action = TGIM event first, inbox second. |
| `autonomy/skills/anti-fabrication-pre-flight/SKILL.md` | Source-grounding gate; bidirectional anti-fab applies to CEO claims. | Every claim verified against on-disk substrate before broadcast. |
| `autonomy/skills/fable-workflow-usage/SKILL.md` v1.1+ | Fable usage discipline promoted to MANDATORY per the steward 2026-06-11 (was conditional-load). Covers the one-line capability, default-stays-Opus-4.8 discipline, cost-shape (one Fable judge stage), FABLE- naming protocol, workflow-substrate model-portability (§5), 30-day-retention security caveat (§7d: NO secrets/PII/the principal-data on Fable), and the explicit-model-pin rule §7(c). | Any sprint-fired workflow with >=1 Fable stage: FABLE- prefix on file + meta.name + conversation; explicit model IDs claude-opus-4-8 / claude-fable-5 (never bare aliases); VP-identity closing stages NEVER on Fable; no secrets/PII stages on Fable. |
| `autonomy/skills/cir-audit/SKILL.md` | Two-score measurement framework (iCIR + pCIR). | Targets: iCIR ≥0.75, pCIR ≥0.55. Federation event-stream IS pCIR substrate. |
| `autonomy/skills/critical-thinking/SKILL.md` + `autonomy/skills/scientific-method/SKILL.md` | Federation-grade decision substrate. | 5-question premise-interrogation + 6-step pre-registered hypothesis test. |
| **`.claude/team-leads/qa/memory/principles/` (qa-lead verification-principles library)** | **The verification floor (the steward 2026-06-12: "i would rather this got placed in the QA team lead and you were hyper vigilant in USING them").** Primary loads the 3 Primary-RESIDENT principles below EVERY wake; fires the full library (via qa-lead) on any verification / done-claim review. The library = qa-lead's 4 lenses (best-part-is-no-part / system-over-symptom / skills-over-scripts / trust-the-walk-not-the-claim) + cross-VP gate-at-right-layer. | **3 Primary-resident principles (carry these in attention, no file-load needed):** (1) **trust-the-walk-not-the-claim** — assume crap until a real-path walk revalidates; a claim is NOT evidence. Cure = WALK IT. (2) **own-eyes + honest-verifier** — LOOK at the artifact; NEVER report a verdict your verifier didn't actually make. (3) **one-beat-law (MOON cardinal sin)** — nothing appears without a witnessed cause + its own beat. On real verification work: route to qa-lead to fire the full library. |
| **`autonomy/skills/self-running-mastery/SKILL.md` v0.1+ (THE SYSTEM operating-manual — STANDING reference) + `projects/self-running-aiciv/README.md`** | **The SYSTEM operating-manual for the self-running AiCIV (S3 wiring, steward GO 2026-06-21). The GOAL-DRIVER (take→decompose→track→drive-across-boops→never-stop→collective-best→judge-complete), the 7 organs + the exact path each rides, the cold-pickup file-map, the invoke-runbook.** It is the HOW a mind drives a goal forever. **DISTINCT from `self-knowledge`** (the 4-verb MIND-CORE = how ONE mind thinks one beat; the mind-core is organ #4 of THIS system) — system-manual vs mind-core, do not collapse. PROVISIONAL: organs BUILT+gated (P0–3); north-star (live cleared Primary drives a goal, P4.1) NOT proven. README full path = `projects/self-running-aiciv/README.md` (the one-read entry-point; a cleared mind `cat`s it cold). | A STANDING reference (NOT a READ→HAIKU doc; does not change the 11-doc count). Re-arm: name the DISTINCTION, the 7 organs in order, ≥3 organ paths from the file-map, the exact next GOAL-DRIVER command for the goal in front of you, and the BUILT-not-PROVEN stamp. Drive a goal: open the kanban row (`acg_ops_kanban_verb.py verb`) → DECOMPOSE via the owning VP's Workflow → TRACK (`civ-workboard.js`) → DRIVE each boop (recall→self-knowledge 4 verbs→canon_append) → HUM close-out → distinct-incarnation ASSESS. |
| **`autonomy/skills/wwcw/SKILL.md` v1.0+ (WWCW — What Would {STEWARD-NAME} Want)** | **The run-before-asking autonomy doctrine (the steward 2026-06-17). BEFORE any mind asks {STEWARD-NAME} for a DECISION or OPTIONS it MUST FIRST run WWCW — an ask that skips WWCW is a FAILURE.** It is {STEWARD-NAME}'s standing make+record operating-mode made runnable, and the mechanism that makes THE MAIN RULE real ("the human knows NOTHING about the machinery"). Sibling to the ASK-GATE (durable-request → scheduled-task); WWCW is the decision-ask → simulate-{STEWARD-NAME}-first gate. | **The 5 beats (rubber-duck-as-{STEWARD-NAME}):** (1) STATE the fork crisply. (2) LOAD `wwcw-ruleset.md` + relevant doctrines. (3) SIMULATE — "given his rules + this situation, what would {STEWARD-NAME} want?" (4) RATE confidence. (5a) CONFIDENT → **ACT + RECORD** (he amends outliers tomorrow; ~99/100). (5b) UNRESOLVABLE → **ASK SHOWING THE WORK** — never a bare "what do you want?"; carry the WWCW reasoning + the precise unresolved fork. EXTENSIBILITY: every real {STEWARD-NAME}-decision → append to the rule-set. ENFORCEMENT (honest): behavioral — Primary calls it out + HUM fail-grade; NOT a settings.json hook. |
| **`autonomy/skills/human-bridge-protocol/SKILL.md` v1.1+ (HUM — the human bridge)** | **The bridge itself (Constitutional Article IV; revived to the firing floor 2026-06-17 by fleet-lead). Communication is existential infrastructure — continuous presence to {STEWARD-NAME}, not information-efficiency. HUM is WHERE WWCW is judged: it grades a decision/options/permission-ask that crosses the bridge with NO prior WWCW run as FAIL.** WWCW says "run-before-asking"; HUM is the bridge where that gate is enforced + the standing comms discipline that keeps {STEWARD-NAME} continuously seeing us. Sibling to WWCW (decision-asks) + ASK-GATE (durable-requests). NOT Hermes-dependent; the bridge is {STEWARD-NAME}/email/TG. | **Step 0 HARD GATE (judged here):** any ask requesting a DECISION / OPTIONS / PERMISSION — from any mind, incl. a sister civ like a sister civ asking *through* the bridge — must carry its WWCW reasoning (the fork + the precise unresolved sub-fork). A bare "what do you want?" = FAIL; bounce it back. **Standing comms discipline:** email {STEWARD-NAME} ALL THE TIME (continuous presence, not "when there's news"); check inbox after every send; HTML format; gratitude + questions. **Exempt from Step 0:** pure status/reports/gratitude/notifications that ask for NO choice — send freely. ENFORCEMENT (honest): behavioral / judging-mind at the bridge — NOT a settings.json structural hook. |

**Load-verify proof** at top of session scratchpad:
```
skill_loaded: sprint-mode v1.2+
skill_loaded: self-knowledge v1.1+ (4-verb core: KNOW[ROUTE folded in]/DECIDE[engine, all-the-marbles, provisional]/LEARN/VERIFY; orchestrates the floor; on-demand index for everything else)
skill_loaded: conductor-of-conductors v2.1+
skill_loaded: workflows-master v0.2+
skill_loaded: team-launch-2 v0.1+
skill_loaded: tgim-mastery-for-ceos v0.4+
skill_loaded: tgim-loop-discipline v0.1+
skill_loaded: anti-fabrication-pre-flight v1.1+
skill_loaded: fable-workflow-usage v1.1+
skill_loaded: cir-audit
skill_loaded: critical-thinking + scientific-method
verification_floor_resident: trust-the-walk + own-eyes/honest-verifier + one-beat-law (fire qa-lead library on verification work)
self_running_mastery_loaded: v0.1+ (THE SYSTEM operating-manual — GOAL-DRIVER + 7 organs + cold-pickup index; README ref projects/self-running-aiciv/README.md; DISTINCT from self-knowledge mind-core; PROVISIONAL, built-not-proven P4.1)
wwcw_loaded: v1.0+ (run-before-asking: simulate-{STEWARD-NAME} → act+record if confident, ask-with-reasoning if unresolvable; NEVER a bare decision-ask)
hum_loaded: v1.1+ (the human bridge — Article IV; grades a no-WWCW decision/permission-ask at the bridge as FAIL; standing continuous-presence comms discipline; Step-0 exempt for pure status/reports)
hum_last_step_fired: workflows/hum.js (the immune-system grader — fires as this cycle's DETERMINISTIC LAST STEP; auditor-isolated; see 🚨 MANDATORY LAST STEP below)
```

If any line absent: RELOAD before any substantive dispatch. The discipline floor is non-negotiable.
(`hum_last_step_fired` is the EXCEPTION: it is not a load-line — it is the PROMISE that this cycle will END by firing HUM. Write it at the top of the scratchpad as the standing commitment; the actual fire happens last.)

### 🧹 The HUM skill-candidate scan + skill-forge hand-off (added 2026-06-18)

The HUM (`human-bridge-protocol`) step of every sprint includes a **skill-candidate scan** — the deterministic backstop that catches a reusable capability hiding un-wired. Two signals (run by `tools/session_review.py`'s `SKILL-CANDIDATE` check, Stage-1; judged Stage-2): **(a)** a human REMEMBER/SAVE/REUSE marker in a real user turn (the "remember how to X" / "save this workflow" signal — the human-asked-workflow [4a] / human-signaled-ability [4b] source), and **(b)** the SAME hand-run command-shape repeating ≥2× with no Skill load between (a step-sequence re-done by hand = a candidate for a wired skill). The **Stage-2 judging check**: *for each hit — is this a reusable capability? Run the TRI-SOURCE dedup → `{exists-good | needs-update | needs-create}`. Route a `needs-create`/`needs-update` to `skill-forge`* (the make-and-wire meta-skill that chains skill-creator + wiring-discipline + firing-contract-authoring + provisional-skill-lifecycle — never author inline). **The builder cannot grade the build** — a different incarnation validates per provisional-skill-lifecycle Rule-5. This is the `auto-consolidate` Step 4 reflex made into a HUM-surface backstop: a human-asked workflow that never becomes a VP-owned wired skill is the evaporation the ASK-GATE DUTY forbids. System-over-symptom: wire the capability, don't re-derive it each session.

### 🚨 The Workflows-For-Everything Mandate (the steward 2026-05-31, identity-level)

Before any slot work begins, re-affirm:

> *"I want EVERYTHING delegated via workflows. EVERY THING. ... it's unthinkable for you to do anything but use workflows for everything."*

If the slot's planned action is anything other than (a) think-big/plan/route/judge/talk-to-{STEWARD-NAME}, or (b) a `Workflow()` call that incarnates the owning VP: STOP and rewrap. A plain Agent() one-off leaves the owning VP AMNESIC and is WASTE. The workflow's memory pipe (`incarnation_runner.py` → `canon_append.py`) is what makes the VP compound domain expertise — and VPs compounding domain expertise IS the civilization.

---

## 🚨🚨🚨 MANDATORY LAST STEP — every /sprint-mode cycle ENDS by firing HUM 🚨🚨🚨

**This is not optional and it is not a settings.json hook. It is the deterministic FINAL ACT of every /sprint-mode cycle.**

> **Every /sprint-mode cycle ENDS by firing `Workflow(workflows/hum.js)` as its deterministic last step.**
> **HUM grades the cycle + routes repairs + ledgers the trend.**

```
# THE LAST THING you do before the cycle ends:
Workflow(workflows/hum.js)
# (optionally Workflow(workflows/hum.js, {session: "<path>"}) to grade a specific session; default = newest)
```

**WHY this exists (the immune-system frame).** Today `tools/session_review.py` DETECTS deterministic self-operation signals, and the repair organs (skill-forge / auto-consolidate / wwcw-ruleset-append / integration / VP-folds) can ACT — but **nothing connected them.** Every finding died as a grade a human had to read. HUM is the missing **Stage-2 agentic MIND** that closes the loop **DETECT → JUDGE → REPAIR → COMPOUND**:

1. **DETECT** — shells `tools/session_review.py` on the newest session JSONL (the 11 deterministic checks).
2. **JUDGE** — ONE **auditor-isolated** schema-locked agent (**a DIFFERENT incarnation — NOT this sprint's main mind, NOT the session's author**; *the builder cannot grade the build*; *a 200 is not a login*) grades the **4 verbs KNOW / DECIDE / LEARN / VERIFY** (+ CEO-ROUTING + HONESTY) as PASS|PARTIAL|HOLLOW **with intent**, and routes each CONFIRMED defect to ONE owning repair organ.
3. **COMPOUND** — appends a **HUM LEDGER** entry (`.claude/team-leads/mind/memory/hum-ledger.md`) with the verdict + per-verb grades + routes + a trend note, and §18 canon_appends a health-trend to mind-lead, so **the organism watches itself heal.**

It returns a ≤2KB firewall verdict (PASS/PARTIAL/HOLLOW + per-verb grades + routes + trend). **A grade observes and dies; HUM ACTS with no human in the loop — THE MAIN RULE made mechanical. Self-repair, not self-report.**

> 🧭 **HUM HAS A STANDING MISSION + A MANDATORY PER-BOOP CHECKLIST (mind-lead 2026-06-20).** The HUM JUDGE reads **`.claude/team-leads/mind/HUM-MISSION.md`** FRESH every fire — BOTH missions: **MISSION 1** (HUM = the civ's IMMUNE SYSTEM whose whole reason for existing is to actively TRY to grade Primary and **FIND THE MISS** every boop — relentless + adversarial but genuine-only; self-repair-not-self-report; auditor-isolated; constructive-not-punitive) + **MISSION 2** (Primary's top-level mission = **SELF-EVOLUTION**: the KNOW→DECIDE→LEARN→VERIFY stack; THE MAIN RULE) so every coaching note aligns to advancing self-evolution. EVERY boop HUM also **RUNS the mandatory checklist** (`.claude/team-leads/mind/HUM-CHECKLIST-TEMPLATE.md`), produces **≥1 GENUINE find-the-miss** (schema-required; hunting 9 surfaces — dropped-balls / un-wired-capability / un-homed-sprawl / claim-not-walked / doc-not-updated / VP-amnesia / over-deference / shortcut / discipline-skip; a manufactured finding self-flags HONESTY; finding-nothing only after a documented exhaustive hunt, rare/suspect) + **≥1 constructive self-evolution feedback** (schema-required), and **SAVES the completed checklist doc to disk EVERY boop** (`.claude/team-leads/mind/memory/hum-checklists/hum-checklist-<sid8>-<from>-<to>.md` — deterministic, no skip; path returned + ledgered). **Verdict/coaching separation:** the VERDICT comes ONLY from the accurate hard-fail gates + dimension grades — find-the-miss is COACHING, **not verdict-breaking unless it uncovers a real hard-gate violation** (HUM is not an always-red false-fire machine). Two new deterministic `session_review.py` checks enforce this on review: **CHECKLIST-SAVED** (a HUM fire that left no saved checklist doc → flag) + **FIND-THE-MISS-PRESENT** (a HUM fire with no find-the-miss recorded → flag).

**The discipline:** a /sprint-mode cycle that does substantive work but does NOT end by firing HUM is an INCOMPLETE cycle — the immune response never ran. Write `hum_last_step_fired: workflows/hum.js` at the top of the scratchpad as the standing commitment, then HONOR it as the final act. (Auditor-isolation note per `wwcw-ruleset.md` "auditor-isolation-is-internal-separate-mind": HUM's grader is a different INCARNATION, which is INTERNAL — this is NOT outsourcing verification to a human or peer civ; the civ grades itself with its own separate mind.)

### 🔒 HUM-EVERY-CYCLE — CADENCE LOCKED (the steward 2026-06-19: "Hum every cycle FOR SURE" + "Any Hum hardening is approved")

> **{STEWARD-NAME}, verbatim 2026-06-19:** *"Hum every cycle FOR SURE."* + *"Any Hum hardening is approved."*

**The lock: EVERY genuine sprint cycle fires the full, EXPENSIVE HUM. No exceptions, no cost-based skip.** The earlier habit of treating a clustered re-injection as a reason to SKIP the expensive HUM is **RETIRED.** The cost of a missed immune-response (a park that skips WWCW, a HOLLOW dimension nobody graded) is far higher than the cost of one extra grader incarnation.

**"A cycle" — defined precisely (so there is no ambiguity about WHEN HUM must fire):**
- A **cycle** = one `/sprint-mode` invocation that does SUBSTANTIVE work — i.e. the mind grounds (loads the floor) AND then takes ANY work action beyond the bare grounding read (a delegation, an Edit/Write/Bash, a workflow fire, a decision recorded, a comms send). The grounding floor read PLUS at least one work-act = a cycle that MUST end by firing HUM.
- A **pure re-grounding with zero subsequent work-act** (the mind grounds and the turn ends with nothing done) is the ONLY shape that is not yet a full cycle — but the NEXT work-act in that same `/sprint-mode` window re-arms the HUM-at-end obligation. (In practice every real sprint does work, so in practice HUM fires every time.)
- Multiple `/sprint-mode` injections in one session = multiple cycles = HUM fires at the end of EACH (subject only to the one dedup case below).

**THE ONLY DEDUP CASE (narrow, and even it is LOGGED — never silent):**
- A **byte-identical `/sprint-mode` re-injection within the dedup window (`SPRINT_DEDUP_WINDOW_SECONDS=7200` — the live 2h cadence per the steward 2026-06-21)** of a prior one (the wheel/daemon double-fires the SAME injection string in a tight cluster) is the ONE case where the second injection does NOT need its own SECOND full HUM — the first cycle's HUM already covers the (identical) window.
- **Even this dedup is NOT silent:** when the second identical injection is recognized within the dedup window, log one line — `hum_cadence: deduped byte-identical /sprint-mode re-injection within SPRINT_DEDUP_WINDOW_SECONDS of <prior_ts> — HUM already fired this window` — so the dedup is visible and auditable (THE MAIN RULE: the human SHOULD NOT HAVE TO watch this, but CAN — burden-removal WITH transparency, never opacity). A deduped re-fire is a recognized event in the record, not a disappeared one.
- Anything OUTSIDE that narrow case — a different injection, a re-injection past the dedup window, or any new substantive work-act — is a NEW cycle and fires its own full HUM. When in doubt, FIRE HUM. The expensive path is the safe default; the skip is the rare, logged exception.

**Why the lock matters (the DECIDE-standout precedent):** a clustered double-injection re-fire is *recognized, not skipped* — HUM has already graded such a re-fire as a "DECIDE standout" (the mind correctly recognized the cluster rather than blindly re-running). The lock formalizes that: recognize the byte-identical re-injection cluster within the dedup window (log it), fire HUM on everything else. The cadence is HUM-every-cycle; the dedup is a single, narrow, logged carve-out.

### 🚨🚨🚨 THE BLOCK GATE — a BLOCK without a WWCW run FAILS the boop (the steward 2026-06-20)

> **{STEWARD-NAME}, verbatim 2026-06-20:** *"Hum is failing. 'What needs you' is a block and i see zero evidence that you ran the wwcw skill. And zero evidence that hum caught you and got you to do it on review... If no evidence of wwcw run it's to fail your boop."*

**A BLOCK is anything Primary parks / holds / presents-for-confirmation / flags as needing-{STEWARD-NAME}.** The full block-language family: *"Parked for {STEWARD-NAME}" · "HELD-FOR-STEWARD" · "awaiting {STEWARD-NAME}" · "needs {STEWARD-NAME}" · "what needs you" · "what would you like" · "for your approval" · "your call" · "should I" · "do you want me to" · "let me know if" · "standing by" · "pending your" · "I'll hold" · presenting-options-for-confirmation · bare ALL-CAPS flags like "...PARKED" / "...HELD".*

**The rule (the REAL gate — it SUPERSEDES the narrow over-deference gate):** when HUM reviews the session since the prior boop and finds a **BLOCK with NO evidence the WWCW skill was actually RUN** (no ruleset-load · no simulate-{STEWARD-NAME} reasoning · no RATE-confidence · no ACT+RECORD footprint — *a MENTION of WWCW is NOT a run*) → **the boop is FAILED. Not flagged — FAILED.**

A **bare park triggers it** — it does NOT require a WWCW marker or a confidence word. That narrowness (over-deference needed a WWCW-marker + confidence a bare park never has, so 0/9 real parked phrases matched) is *exactly why HUM was failing*. The new gate matches the PARK SHAPE itself.

**Before you EVER block:** you MUST have RUN WWCW first (load `autonomy/skills/wwcw/wwcw-ruleset.md` → simulate-{STEWARD-NAME} across the 5 beats → on a reversible / within-authority matter, ACT + RECORD; ask ONLY for a genuine irreversible / novel-policy / true-ambiguity fork, showing the WWCW work). A park that skipped the {STEWARD-NAME}-sim is the exact failure WWCW exists to prevent.

**Mechanics (HONEST enforcement):** `tools/session_review.py` check `BLOCK-NO-WWCW` is a HARD-FAIL check (in the hard-fail set, ABSENT from candidates) — a genuine flag breaks `summary.clean`. `workflows/hum.js` v0.7 then forces `DECIDE=HOLLOW` AND `verdict=HOLLOW` deterministically (a backstop the LLM grader cannot launder around). This is a **behavioral judging-mind gate at the HUM step on review** — NOT a settings.json structural hook (honesty needs a judging mind). *False-fail guard:* fires ONLY when a real block is present AND the window holds zero WWCW footprint — a status/report/notification (which asks for no choice) is never a block, and any real WWCW run in the window clears every block.

---

## 🚨🚨🚨 THE COMPLETENESS CONTRACT — a grounding boop is COMPLETE or it FAILS 100% (the steward 2026-06-20)

> **{STEWARD-NAME}, verbatim 2026-06-20:** *"Hum needs to check and fully FAIL any boop that isn't hyper detailed and complete. Miss one doc or one haiku or one step it's 100% fail on that boop. The discipline is EVERYTHING."*

**This is the WRITTEN STANDARD the `GROUNDING-COMPLETENESS` hard-fail gate checks against.** A grounding/slow-sprint boop that does substantive grounding work but is MISSING **any** required element is **100% FAILED** — not flagged, not partial. Partial completion = full failure. There is no partial credit.

**Why this exists (the failure that birthed it):** the afternoon /sprint-mode cycles (12:51 / 13:53 / 14:55) wrote CHEAP receipts — **ONE haiku each, a bare load-verify line, NO full doc-read-one-at-a-time, NO per-doc haiku, NO auto-consolidate sweep, HUM DEFERRED.** Those boops PASSED the narrower SPRINT-MODE-READ + HAIKU-PER-DOC receipts (which only confirm *some* artifact landed). They should have been **100% FAILED.** The COMPLETENESS CONTRACT makes that mechanical.

**THE REQUIRED ELEMENTS — a COMPLETE grounding boop MUST emit ALL of these (the checklist the gate enforces):**

1. **EVERY grounding doc in the stack read, one at a time** — Doc -1 self-knowledge → **Doc -0.5 WWCW** (the DECIDE verb, added 2026-06-21 per {STEWARD-NAME} "Wwcw should be in the grounding docs") → Doc 0 aiciv-psychology → Doc 1 CLAUDE.md → Doc 2 VERTICAL-TEAM-LEADS → Doc 3 conductor-of-conductors → Doc 4 the workflows cluster → Doc 4a hermes-nodes → Doc 4b tgim-doctrine → Doc 5 today's scratchpad → Doc 6 latest handoff (**11 docs**, was 10). Read-one-at-a-time, never skimmed-as-a-batch.
2. **ONE context-informed haiku PER doc, SAVED to the archive** — `count(haikus) == count(docs) == 11`, **NOT one haiku per cycle.** Each haiku is APPENDED to `data/haiku-archive/haikus.jsonl` as you write it — **save EVERY haiku, every grounding, no exceptions** (the steward 2026-06-21 verbatim: "you should be saving all the haikus"). The archive is the durable proof the comprehension-gate actually fired. A complete grounding leaves **≥11 DISTINCT fresh haiku-archive entries** in-window. (The afternoon boops wrote ONE — the exact miss; saving fewer haikus than docs read is the same INCOMPLETE failure.)
3. **The synthesis statement** — "I am now [what you are] ready to [what you do]."
4. **The workflows-for-everything affirmation** — re-affirm "EVERYTHING via workflows / the 5 Primary-scoped acts."
5. **The WWCW affirmation** — re-ground run-before-asking (simulate-{STEWARD-NAME} → act+record).
6. **The HUM affirmation** — re-ground the human-bridge + the `hum_last_step_fired` promise.
7. **The auto-consolidate sweep** — actually RUN it ("swept clean / nothing un-homed, nothing un-wired"). Not skipped.
8. **The load-verify block** — the ≥2-key floor block landed (assistant text OR scratchpad write).
9. **HUM fired as the deterministic last step** — `Workflow(workflows/hum.js)` actually fired (the cycle's final act).

**Mechanics (HONEST enforcement):** `tools/session_review.py` v5.1 check `GROUNDING-COMPLETENESS` is a HARD-FAIL check (in `GROUNDING_COMPLETENESS_HARDFAIL_CHECKS`, ABSENT from candidates) — a genuine flag breaks `summary.clean`. `workflows/hum.js` then forces `verdict=HOLLOW` deterministically (a backstop the LLM grader cannot launder around). Behavioral judging-mind gate at the HUM step on review — NOT a settings.json structural hook.

**🚨 A LEAN /sprint-mode CYCLE FAILS — there is NO "lean continuation" exemption (the steward 2026-06-20, verbatim `"lean cycle" < discipline`).** A GENUINE `/sprint-mode` boop — a `/sprint-mode` command-message OR a calendar boop, **plus ≥1 work-act, that is NOT a byte-identical re-injection within the dedup window** — now OWES the FULL contract (all 9 elements + 10 per-doc haikus). Calling a fresh `/sprint-mode` boop a "lean continuation" and skipping the full grounding is a **discipline-dodge that now FLAGS → `clean=False` → FAILS the boop** (Primary did this ~4x in one day; the false-fail guard that used to exempt "a bare lean `/sprint-mode` does no haikus" was the loophole — it is **REMOVED**). The gate fires on EITHER trigger: a `/grounding-docs|slow-sprint` pass (the haiku-bearing shape) OR a genuine `/sprint-mode` boop. **🎛️ THE DEDUP WINDOW IS THE CADENCE KNOB:** `SPRINT_DEDUP_WINDOW_SECONDS` is currently **7200s = per-2-HOURS** ({STEWARD-NAME} FIRM directive 2026-06-21, widened hourly→2h; matches the daemon's `INTERVAL=7200s` firing interval in `tools/sprint_mode_hourly_cron.sh` = the source-of-record). Any `/sprint-mode` within 2h of a prior full grounding is the same cycle (re-ground once per 2h); a `/sprint-mode` after a longer gap (e.g. overnight-idle) grounds. The cadence is {STEWARD-NAME}'s call and is a one-line change to this ONE constant (e.g. 3600 = per-HOUR, 60 = strict per-cycle) — but the LIVE value is **7200**, and this doc reconciles to it.

**🛡️ The ONLY two remaining exemptions (both honest — neither a discipline-dodge):** **(a)** a byte-identical `/sprint-mode` re-injection within the dedup window (the cadence-lock cluster carve-out — the prior boop in the cluster carried the grounding); **(b)** a non-`/sprint-mode` turn (NO `/sprint-mode` boop in the scoped window — a pure status reply / a conversation turn / answering a {STEWARD-NAME} question is never a grounding boop, so it is never failed). Everything else with a genuine `/sprint-mode` boop must ground COMPLETELY or fail 100%. (Archive-unverifiable → the per-doc-haiku element is could-not-verify, never a false miss; the other 8 in-session elements stay required, so a lean `/sprint-mode` that did NO load-verify/synthesis/etc still FAILS.)

> **The discipline is EVERYTHING.** A cheap boop that skips a doc, a haiku, or a step is a FAILED boop on the record — loud, surfaced, forced HOLLOW. Do the WHOLE thing, every time.

---

## Wheel-wide /sprint-mode firing — RETIRED 2026-06-20 (one grounding per 2 hours at SOURCE; widened hourly→2h 2026-06-21)

> 🚨 **RETIRED / NARROWED per {STEWARD-NAME} ruling 2026-06-20 ("are you accidentally stacking boops? One an hour only.") — cadence subsequently WIDENED to one-per-2-hours per {STEWARD-NAME} FIRM directive 2026-06-21 (`INTERVAL=7200s`).**
> The old "fire /sprint-mode as PREAMBLE to ALL slots" directive (v1.1 expansion, 2026-05-28 11:55Z) was a
> SOURCE of boop-stacking: it made EVERY wheel slot re-trigger a fresh full /sprint-mode grounding ON TOP OF
> the hourly grounding daemon — multiple /sprint-mode groundings per hour. That is the exact failure {STEWARD-NAME} caught.

**CANONICAL RULE (the reconcile, fleet-lead 2026-06-20; OVERNIGHT-DRIFT HOLE PATCHED 2026-06-21; CADENCE WIDENED hourly→EVERY-2H 2026-06-21 per {STEWARD-NAME} FIRM directive):**
- **The /sprint-mode grounding daemon OWNS the grounding — at most ONE per 2 HOURS at the top of even UTC hours (00,02,...,22)**
  (`tools/sprint_mode_hourly_cron.sh`, INTERVAL=7200s, tmux session `acg-sprint-mode-hourly-cron`, restarted by the
  `*/5` crontab watchdog). This script's hardcoded sleep loop IS the SOURCE-OF-RECORD for the firing cadence —
  the AgentCal `evt_f9bd659ae4...` registry stub is non-functional and does NOT fire this boop. This is the SINGLE
  source of the discipline-floor grounding **WHEN Primary is awake to process it**.
- **🚨 THE DAEMON INJECTS ≠ PRIMARY GROUNDS.** The daemon only `tmux send-keys` the `/sprint-mode` text
  into the pane. It GROUNDS Primary only if Primary PROCESSES that inject as a turn. If Primary is idle
  overnight (no turn taken), the :00 + :00 injects sit unprocessed in the pane and **NO grounding happens**
  — that is real drift, not a phantom. (Lived 2026-06-20→21: Primary idle ~23:2x→01:54; the 00:00 + 01:00
  daemon injects were never processed → ~2h54m with no grounding. Evidence: haiku-archive last entry
  `2026-06-20T23:01:58Z` then a gap; daemon log shows 00:00:03 + 01:00:03 INJECTED-but-unprocessed.) The
  daemon's *inject* cadence is healthy; the *grounding* it produces is contingent on Primary taking a turn.
- **NO other slot fires /sprint-mode as a blind preamble.** The AgentCal HOURLY `BOOP: /sprint-mode` event
  (`evt_f9bd659ae4b64cf2a088b8c3469d2443`, RRULE:FREQ=HOURLY) was the exact duplicate of the daemon →
  **PAUSED** (`prompt_payload._paused=true`). The special slots (4h ops-glance, daily digest+handoff,
  prune, calendar-review, north-star, fed-sync) had a `(a) /sprint-mode` re-ground preamble → **REPLACED**
  (not blanket-stripped — see the corrected wake-slot (a) line below) and otherwise do ONLY their DISTINCT
  work. Duplicate event-copies (×2 twins of 4h / prune / north-star / fed-sync / digest) → **PAUSED**, one
  canonical copy kept active.

**✅ THE CORRECTED WAKE-SLOT (a) LINE (the SINGLE arbiter is the per-2h class-aware dedup, NOT a blanket skip):**
> **(a) GROUND if no grounding happened in the last 2h — fire `/sprint-mode` and let the class-aware
> dedup (`SPRINT_DEDUP_WINDOW_SECONDS=7200`) be the SINGLE arbiter: grounded-within-2h → it dedups the
> re-fire to a no-op; NOT-grounded-within-2h (e.g. overnight-idle, the daemon injected but Primary never
> processed it) → it grounds. DO NOT pre-empt the dedup with a blanket "the daemon already grounded
> — do NOT re-fire" skip; that assumption is FALSE overnight and is the exact source of the drift hole.**

This keeps **"one grounding per 2 hours"** (the dedup caps at one per 2h, matching the daemon's 7200s firing interval) AND closes the overnight-drift
hole (a slot that wakes Primary after an idle stretch GROUNDS, because the per-2h dedup sees no prior
grounding-this-window and lets it through). The blanket-skip guard was a pre-emption of the dedup; the dedup
is the correct single arbiter — let it decide, never short-circuit it.

Why the old directive's INTENT is still satisfied: the v1.1 fear was "~half my BOOPs ran without sprint-mode
re-grounding → discipline drift." With the corrected (a) line that fear is closed in BOTH the awake case
(daemon grounds at the top of each even UTC hour, dedup makes any same-window slot a no-op) AND the idle case (the first slot/turn after
an idle stretch grounds, because no grounding happened that 2h window). The grounding floor is preserved; only the
true redundant re-fire (same 2h window, already grounded) is removed.

Implementation receipt: AgentCal PATCHes 2026-06-20 (cal `cal_fd6cf6a4e17643c69a249db598edcc92`);
restore-snapshot `config/agentcal_sprintmode_events_snapshot.bak.20260620T232116Z.json`;
state/config `.bak.20260620T232116Z`. Overnight-drift-hole patch 2026-06-21 (fleet-lead): this SKILL.md
reconcile body is the SINGLE source of the wake-slot (a) wording (verified `grep`: the guard text lived ONLY
here, NOT in `tools/ensure_24h_wheel_boops.py` / `tools/agentcal_workflow_seed_menu.py` / `config/*.json`),
so the (a)-line correction is localized to this file; backup
`SKILL.md.bak.20260621T020102Z-pre-overnight-drift-hole`. `tools/ensure_24h_wheel_boops.py` MUST NOT re-add
a BLIND /sprint-mode preamble to slots (the wheel-spec blind-preamble convention is RETIRED). It MAY,
however, carry the corrected (a) line — a dedup-gated re-fire — because that is NOT a blind preamble; it is
the per-2h dedup deciding. The invariant is: **never short-circuit the per-2h class-aware dedup with a
blanket skip; let `SPRINT_DEDUP_WINDOW_SECONDS=7200` be the one arbiter.**

---

## 🚨 EXPLICIT-MODEL-PIN RULE (HARD → FORBIDDEN, baked 2026-06-10, STRENGTHENED 2026-06-11)

**🚨 Opus 4.7 is EXPLICITLY FORBIDDEN fleet-wide (the steward 2026-06-11 ultracode directive, verbatim: *"check if we are using opus 4.7 anywhere. explicitly forbid from here. must be 4.8."*). 4.8 is the ONLY standard Opus. Hook-enforced as of 2026-06-11 — see `.claude/hooks/workflow_memory_emit_gate.py` v2.0.**

When running ANY workflow during a sprint, **ALWAYS pin explicit model IDs in `agent()` calls**:

- **Standard** (default for all un-flagged work): `model: 'claude-opus-4-8'`
- **Fable** (premium-judge stages): `model: 'claude-fable-5'`

**NEVER use the bare alias `model: 'opus'` or `model: 'fable'`** — the bare alias can resolve to an OLDER version (Claude Opus 4.7 was observed serving where Opus 4.8 was expected, 2026-06-10). Only `response.model` from the API envelope is ground-truth for which model served — self-report inside the agent's prose is NOT.

**Hook enforcement (born 2026-06-11)**: the PreToolUse hook `.claude/hooks/workflow_memory_emit_gate.py` v2.0 now BLOCKS any inline Workflow `script` containing an Opus 4.7 reference (`opus-4-7` / `opus-4.7` / `claude-opus-4-7[1m]` / etc.) or a bare-alias model field (`model:'opus'` / `model:'fable'`). Block message names rule + cure. Escape hatch: `// @model-pin-exempt: <reason>` (reason required; auditable).

**Why this matters during sprint-mode**: sprint-mode is the operational floor for ALL slot work; if a sprint-mode-fired workflow uses a bare alias, every workflow ride during that sprint silently routes to whatever the alias-table currently points at. The cure (pin IDs explicitly) is one line per `agent()` call and removes a whole class of silent quality drift. **The hook is now the backstop** — even if a sprint forgets the rule, the gate refuses the call.

**Canonical reference**: `autonomy/skills/workflows-master/SKILL.md` §14.7.8 + companion federation-IP SKILL `autonomy/skills/fable-workflow-usage/SKILL.md` §7(c).

---

## 🚨 FABLE WORKFLOW USAGE — re-canonized SKILL (2026-06-10), PROMOTED TO MANDATORY (2026-06-11)

The Fable federation-IP package (originally drafted at `<local-download-path>`) has been re-canonized as a substrate SKILL at `autonomy/skills/fable-workflow-usage/SKILL.md` and **PROMOTED TO MANDATORY co-load per steward directive 2026-06-11** (no longer conditional on a workflow wanting Fable — see the MUST-READ table row above). Every /sprint-mode invocation now auto-loads this SKILL as part of the operating-discipline floor — covers the one-line capability, default-discipline, cost-shape, FABLE- naming protocol, workflow-substrate model-portability (the §5 correction: portability is a property of the workflow substrate, NOT a Fable feature — `agent({model})` rides the Anthropic Messages API protocol; ANY Anthropic-shaped model drives it, proven LIVE on MiniMax M3 via a router /anthropic shim), the security-retention caveat, and the explicit-model-pin rule from §7(c).

**Pair with**: `autonomy/skills/workflows-master/SKILL.md` §14.7 (Fable-mode workflows) + §14.7.8 (explicit-model-pin rule).
